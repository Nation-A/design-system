---
"@nation-a/ui": patch
---

fix: restore Spinner export to main entry point

- Add Spinner back to default exports for backward compatibility
- Spinner is still available from both `@nation-a/ui` and `@nation-a/ui/lottie`
